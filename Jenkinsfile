agent any
stages {
  stage('Build') {
    steps {
      // One or more steps need to be included within the steps block.
      echo "Build"
    }
  }

  stage('Test') {
    steps {
      // One or more steps need to be included within the steps block.
      echo "Test"
    }
  }

}

