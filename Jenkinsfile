stages {
  stage('Build') {
    agent any
    steps {
      // One or more steps need to be included within the steps block.
      echo "Build Stage" 	
    }
  }

  stage('Test') {
    steps {
      // One or more steps need to be included within the steps block.
      echo "Test Stage"
    }
  }

}

